Programme to switch backgrounds.
Functions should include:
    Saving a "collection" of background images and putting through as a slideshow
    Switching backgrounds every so often according to user input
    Saving the last background shown if user swaps "collection"
